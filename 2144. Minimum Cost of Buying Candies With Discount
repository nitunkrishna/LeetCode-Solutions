class Solution
{
public:
    int minimumCost(vector<int>& cost)
    {
        int res=0;
        for(int i=0; i<cost.size(); i++)
        {
            int x=i+1;
            sort(cost.begin(), cost.end(), greater<int>());
            if(x%3==0) continue;
            else res+=cost[i];
        }
        return res;
    }
};
