class Solution
{
public:
    vector<bool> prefixesDivBy5(vector<int>& nums)
    {
        vector<bool>res;
        int rem=0;
        for(int i=0; i<nums.size(); i++)
        {
            rem=((2*rem)+nums[i])%5;
            res.push_back(rem==0);
        }
        return res;
    }
};
